%h1 Listing posts

%table.table
  %tr
    %th Title
    %th Owner
    %th
    %th
    %th

  - @posts.each do |post|
    %tr
      %td= post.title
      %td= post.author.email
      %td= link_to 'Show', user_blog_post_path(current_user.id, post), :class => "btn btn-success"
      %td= link_to 'Edit', edit_admin_post_path(post), :class => "btn btn-warning"
      %td= link_to 'Destroy', [:admin, post], :method => :delete, :data => { :confirm => 'Are you sure?' }, :class =>"btn btn-danger"

%br

= link_to 'New Post', new_user_blog_post_path
